{
  "name": "@swarmui/workflow-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server for SwarmUI's bundled LTX 2.5 and MiniMax H3 ComfyUI workflows.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  }
}
